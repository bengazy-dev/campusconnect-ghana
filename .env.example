# InsForge — copy to .env, fill values from the dashboard, then: npm run env:generate
# (writes gitignored js/insforge-env.js for the browser)

INSFORGE_BASE_URL=https://YOUR_APPKEY.region.insforge.app
INSFORGE_ANON_KEY=your_anon_key_here
