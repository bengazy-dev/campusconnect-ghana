# InsForge — run once: npm run setup:env   (creates .env if missing + regenerates js/insforge-env.js)
# Replace placeholders with values from your InsForge project dashboard. Never commit .env.

INSFORGE_BASE_URL=https://YOUR_APPKEY.region.insforge.app
INSFORGE_ANON_KEY=your_anon_key_here
