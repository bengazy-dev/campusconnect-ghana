/* Example client config. Copy to .env (see .env.example) and run: npm run env:generate */
window.INSFORGE_CONFIG = {
  baseUrl: "https://YOUR_APPKEY.region.insforge.app",
  anonKey: "YOUR_INSFORGE_ANON_KEY",
};
