{
  "name": "campusconnect-ghana",
  "private": true,
  "type": "module",
  "scripts": {
    "build:config": "esbuild js/insforge-config-source.js --bundle --format=esm --outfile=js/vendor/insforge-config.bundle.js --platform=browser",
    "env:generate": "node scripts/generate-insforge-env.mjs",
    "postinstall": "npm run build:config && npm run env:generate",
    "create-test-accounts": "node scripts/create-test-accounts.mjs"
  },
  "dependencies": {
    "@insforge/sdk": "1.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
